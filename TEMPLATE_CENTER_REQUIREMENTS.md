# 模板中心页面开发需求确认

## 当前状态分析

### 已有组件
- ✅ `/app/templates/page.tsx` - 模板中心页面（使用mock数据）
- ✅ `/components/templates-content.tsx` - 模板内容组件（使用mock数据）
- ✅ `/components/template-selector.tsx` - 模板选择器（已使用真实API）

### 已有API
- ✅ `POST /api/templates/list` - 模板列表查询
- ✅ `POST /api/templates/search` - 模板搜索
- ✅ `getTemplates()` - 前端API函数
- ✅ `searchTemplates()` - 前端API函数
- ✅ `getTemplateThumbnailUrl()` - 封面图URL生成
- ✅ `getTemplatePagePreviewUrl()` - 页面预览图URL生成

## 需要确认的关键问题

### 1. 模板数据结构

**问题**：API返回的模板数据包含哪些字段？

**当前TemplateInfo接口**：
```typescript
interface TemplateInfo {
  pptx_id: string
  file_name: string
  title: string
  download_url: string
  theme_color: string
  page_size: number
  priority: number
}
```

**需要确认**：
- 是否包含 `primary_id` 和 `primary_name`（分类信息）？
- 是否包含其他字段？
- 请提供实际的API响应示例

### 2. 分类处理方式 ⭐ 重要

**问题**：如何获取和展示模板分类？

**选项A**：从模板数据中提取分类
- 从返回的模板列表中提取所有唯一的 `primary_id` 和 `primary_name`
- 动态生成分类按钮
- 优点：自动适应数据
- 缺点：如果第一页没有所有分类，分类列表不完整

**选项B**：固定分类列表
- 使用预定义的分类列表
- 通过 `primary_id` 筛选
- 优点：分类清晰、可控
- 缺点：需要知道所有分类ID

**选项C**：先查询所有分类（如果有分类接口）
- 是否有专门的分类查询接口？
- 如果有，先获取所有分类，再根据分类筛选模板

**请确认**：选择哪种方式？或提供分类查询接口？

### 3. 分页方式 ⭐ 重要

**选项A**：传统分页（页码按钮）
- 显示：`[上一页] [1] [2] [3] ... [下一页]`
- 优点：可快速跳转
- 缺点：需要知道总页数

**选项B**：无限滚动
- 滚动到底部自动加载
- 优点：体验流畅
- 缺点：无法快速跳转

**选项C**：加载更多按钮
- 显示"加载更多"按钮
- 优点：用户可控
- 缺点：需要多次点击

**请确认**：选择哪种方式？

### 4. 每页显示数量

**建议**：
- 桌面端：12个（3列×4行）或16个（4列×4行）
- 平板：8个（2列×4行）
- 移动端：4个（1列×4行）

**请确认**：每页显示多少个模板？

### 5. 模板预览功能 ⭐ 重要

**选项A**：只显示封面图
- 简单快速
- 无法查看内页

**选项B**：多页预览（可翻页）
- 显示封面图
- 支持翻页查看各页预览图
- 需要调用 `getTemplatePagePreviewUrl(pptx_id, pageNumber)`

**选项C**：封面图 + 缩略图列表
- 大图显示封面
- 下方显示所有页面缩略图
- 点击缩略图切换大图

**请确认**：选择哪种预览方式？

### 6. 模板选择后的行为

**当前实现**：
```typescript
router.push(`/workspace?templateId=${templateId}`)
```

**需要确认**：
- 跳转后，工作台是否自动选中该模板？✅（已实现）
- 是否需要显示提示信息？
- 是否需要自动填充主题？

### 7. 搜索功能

**当前实现**：
- 搜索框输入关键词
- 按Enter或点击搜索按钮
- 调用 `searchTemplates()` API

**需要确认**：
- 搜索时是否清空分类筛选？
- 搜索无结果时的提示文案？
- 是否需要搜索历史记录？

### 8. UI/UX细节

**需要确认**：
- 模板卡片的尺寸和比例（建议：3:2或16:9）
- 加载状态：骨架屏/加载动画/其他？
- 空状态文案和样式
- 错误提示文案和样式
- 响应式断点（移动端/平板/桌面）

## 建议的默认方案

如果暂时无法确认，建议使用以下默认方案：

1. **分类**：选项A（从数据中提取分类）
2. **分页**：选项C（加载更多按钮）
3. **每页数量**：12个（3列×4行）
4. **预览**：选项B（多页预览，可翻页）
5. **搜索**：清空分类筛选
6. **加载状态**：骨架屏
7. **空状态**：友好的提示文案

## 下一步

请确认以上8个问题，特别是标记⭐的重要问题。确认后我将开始实现。
